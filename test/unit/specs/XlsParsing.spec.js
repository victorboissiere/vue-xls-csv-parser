describe('Test xlsx files are correctly parsed', () => {
  it('should correctly parse all xls files', () => { // eslint-disable-line
    /*
    return parseFile(new Blob(['truc'])).then((result) => {
      expect(typeof result).to.equal('object');
    });
    */
  });
});
